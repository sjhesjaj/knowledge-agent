# 评测报告

评测日期：2026-07-20

本报告记录企业知识库 RAG Agent 在当前代码版本上的可复现结果。测试资料为 `sample_company_rules.md`，包含 20 个制度知识块；模型通过本地 Ollama 运行。

## 环境

- Chat Model：`qwen3:4b`
- Embedding Model：`nomic-embed-text`
- 文档：1 份模拟员工手册，20 个知识块
- 有答案检索题：80 条（两组各 40 条）
- 无答案题：20 条
- Agent 路由题：15 条

## 复现命令

```powershell
# 一次运行全部检索、路由和拒答评测
.\run_tests.ps1

# 自动化单元与接口测试
.\.venv\Scripts\python.exe -m unittest discover -v

# 前端构建
cd frontend
pnpm run build
```

## 检索评测

第一组 `eval_cases.json`：

| 方法 | Recall@1 | Recall@3 | MRR |
|---|---:|---:|---:|
| 纯向量 | 45.0% | 52.5% | 0.542 |
| 纯 BM25 | 90.0% | 97.5% | 0.938 |
| RRF 融合 | 60.0% | 82.5% | 0.715 |

第二组 `eval_cases_extended.json`：

| 方法 | Recall@1 | Recall@3 | MRR |
|---|---:|---:|---:|
| 纯向量 | 32.5% | 65.0% | 0.515 |
| 纯 BM25 | 92.5% | 97.5% | 0.954 |
| RRF 融合 | 70.0% | 87.5% | 0.810 |

两组共 80 条问题的汇总结果：

| 方法 | Recall@1 | Recall@3 | MRR |
|---|---:|---:|---:|
| 纯向量 | 38.8% | 58.8% | 0.529 |
| 纯 BM25 | 91.3% | 97.5% | 0.946 |
| RRF 融合 | 65.0% | 85.0% | 0.763 |

### 结论

- 在当前中文制度数据上，BM25 明显强于所用通用 Embedding，因此系统保留 BM25 高置信快速路径。
- RRF 在扩展问法上的 Recall@1 优于第一组，但总体仍低于 BM25，说明后续应评估中文 Embedding 或 Cross-Encoder。
- 汇总数字用于召回方案比较；线上链路还会继续经过候选重排和答案生成。

## Agent 路由评测

`eval_agent_routes.json` 覆盖普通对话、知识检索、来源列表和知识库总结四类意图。

- Tool Selection Accuracy：100%（15/15）

该数据集规模较小，因此只作为路由回归集，不代表开放场景泛化准确率。

## 无答案拒答评测

`eval_no_answer.json` 包含 20 条知识库中没有明确依据的问题。

最终结果：

- 拒答准确率：100%（20/20）
- 平均端到端耗时：11.48 秒
- 最大端到端耗时：24.07 秒

### 本次回归修复

首次运行当前版本时，首轮模型能够正确拒答，但二次证据复查偶发只复述用户问题，覆盖了原本正确的拒答，导致结果下降到 65%（13/20）。

修复后增加以下约束：

- 重试结果若仍为明确拒答，可以覆盖首轮结果；
- 非拒答结果必须包含 `[来源1]` 引用标记；
- 空答案、问题复述或无引用陈述均被判定为无效，保留首轮拒答。

修复后重新运行同一数据集，结果恢复为 100%（20/20）。对应逻辑和回归测试位于 `rag.py` 与 `tests/test_routing.py`。

## 自动化测试

当前共有 25 项单元和接口测试，覆盖：

- Ollama 结构化流分片、转义、异常和断流
- SSE 成功与失败写入语义
- 客户端会话隔离与越权拒绝
- SQLite 重启恢复、级联删除和模型元数据
- 知识库替换/清空事务原子性
- 知识版本变化时旧回答不落库
- 多文件知识块全局编号
- 复合问题上下文继承
- 证据复查问题复述保护

结果：25/25 通过。

## 结果解释与限制

- 所有数据均为自建模拟员工制度，不应解释为通用企业知识库性能。
- 评测运行于本地 4B 模型，延迟会受硬件、模型缓存和 Ollama 状态影响。
- 20 条拒答题和 15 条路由题不足以证明生产准确率，主要用于防止代码回归。
- 后续应增加答案正确性、引用一致性、P50/P95 延迟、并发吞吐和大规模文档评测。
