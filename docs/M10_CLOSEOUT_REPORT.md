# M10 本阶段收尾验收报告

交付日期：2026-09-15。主验收方：Codex；实现、静态复核及语义复核使用了 Claude 和有界验收子代理，分歧由主验收方按原始证据裁定。

**结论：本阶段工程约束与预设量化门槛通过，按下述范围交付。** top_k/子查询上限、复查回退、最终接口配对、资料更新、文档与恢复快照均已完成。复合选择及语义风险保留；本结论不表示所有复合问题都答对，也不表示生产部署验收完成。

代码保持未提交、未推送、未合并；真实数据库未改动。HEAD 仍是 `7480ba842ae5ea1e07d0125bd9422f9193d3c20b`。不能以该未移动的HEAD代表实际候选，请使用下列运行时哈希与交付清单。

## 1. 改了什么

保留默认 `top_k=4`，严格执行调用方正数证据上限与最多三个召回子查询。合并查询保留原始分句；一次结构化选择可为同一原始分句选多个片段，并保留片段归属。压缩预算优先新增的分句覆盖，再比较相关性；旧的平坦ID响应仍可读取，但不凭空生成归属。BM25快路径逐条检查原始分句，不能用合并后一个高分掩盖别的子问题缺资料。

最终补齐了满额时提前退出的问题：已经占满预算也允许召回池中的强词面匹配证据参与分配。模型语义归属与词面匹配分开计数，避免某个错误的词面首名覆盖正确的语义来源。不会为了补齐数量引入召回池外的证据，也没有增加模型请求。选择仍是有限启发式，不能保证任意输入达到全局语义最优。

`decide_delivery()` 只有在复查正文也通过交付校验时才替换有效首轮答案；错误复查不能抹掉正确首轮，错误首轮加错误复查仍拒答，有效纠正仍保留。普通/SSE共用决策，生成和复查合计最多两次，SSE在完成校验后才发送正文。

相对9月12日接管快照，本阶段运行时只改 `rag.py`；当前文档改 `README.md`、`FRONTEND.md`、`docs/DEMO_GUIDE.md`。新增两份行为测试 `tests/test_closeout_regressions.py`（13项）、`tests/test_full_budget_coverage.py`（6项），以及本报告。`docs/m10-selftest/revert_probe_11.py` 更新了撤销位置/测试目标，既有业务断言未改。

历史累计M10 diff仍包含 planner、两个adapter、编排和原有streaming测试变更。历史批准的逐块断言调整保持原样；“本阶段未改旧断言”不意味着从最初630项基线起从未改过任何测试。

## 2. 源码身份与证据继承

| 文件 | 最终原始字节SHA-256 |
|---|---|
| rag.py | `48f9ecd9c55ab7459b2c06a6cd16d05e85f044d8fbd8cd06ad512ed2c5bc6f79` |
| api.py | `1b91b85fc286ea0db8445a27c6792c903cd29a7aff02a72ad829888bdf3f18ae` |
| chat_orchestration.py | `313db68bcddbc07dddbb77d847c48582669486e861a711cc420ee5be67c9db3d` |
| orchestration/document_adapter.py | `25d5d5e3dc7ffd4a8f6ed04288c8e17998c9368b0ab14aa6ef03826edd495d94` |
| orchestration/planner.py | `64907de73421bf183191e62c6afaf0a907c394ef728ea22d7c881190e7251410` |
| orchestration/wiki_adapter.py | `abe2c91ddc0be2d496266c1c0973e2d16a25c649d6df39a1d5e363e040f1053e` |

接管时 `rag.py=6f31544a…`；广泛真实模型测量完成于中间候选 `8b8fd6b5b7377b9e996868b94f90ffa31ed88170434056a9fced41d8f95d1a5d`。最终48f只修改超过三个原始分句合并后的辅助函数及受该条件保护的调用。完整AST差异审计和112题×原问句/聚焦问句共224次检查证明：公开回答40、独立回答40、相关性20、API12均不会进入改动分支，其余运行时代码不变。

因此广泛回答与相关性结果保留8b身份，作为最终版不受影响路径上的继承证据；不改称最终48f重跑。性能不继承。最终48f实际重跑了完整确定性检查、受影响的复合12题×3轮、新基线/候选真实API配对和上传更新演练。报告完成时只更新文档，运行时与被测副本逐字节一致。

证据根目录：`D:/Users/h000_/Documents/ChatGPT/agent项目改进/m10-acceptance-20260908/closeout-20260912`，下文路径均相对此目录。预设口径为 `ACCEPTANCE_PROTOCOL.md`，裁定与勘误为 `ACCEPTANCE_DECISIONS.md`。

## 3. 环境和题集

Windows 11、RTX 4060 8GB、项目Python 3.12.14、Ollama 0.32.15。qwen3:4b digest为 `359d7dd4bcdab3d86b87d73ac27966f4dbb9f5efdfcc75d34a8764a09474fae7`；nomic-embed-text:latest为 `0a109f422b47e3a30ba2b10eca18548e944e8a23073ee3f3e947efcf3c45e59f`。继承批次、最终复合批次、替代API批次的模型内容相同。推理任务串行运行，API和更新均使用复制源码、独立SQLite/Wiki和空闲端口。

原始资料与公开冻结评测器保持不变。本次独立集针对已曝光题作等覆盖替换，在候选私有集测量前冻结；2026-09-14 05:39:20 UTC的v2清单和曝光/重合审计保留。称“实现方未见”，不称双盲，也不能将替换前后成绩当成纯代码A/B提升。

| 本次冻结输入 | 数量 | SHA-256 |
|---|---:|---|
| routes.json | 80 | `4d34e2e4d8bf8444b39a245f8a15bad4fcc776b0de6012038ec59746dac80ce2` |
| answers.json | 40 | `766728baa968a7260c3da388646bfd2229fa0d8ae8dad29b6a6e48401d609d1a` |
| relevance.json | 20 | `db2c6329c9344e6984250e44c4623077c1be6cec4eaf7953729ec0ee8d1d9165` |
| api-cases.json | 12 | `97e16728e8d745ec87847b684a8b28da64d5ac11d0f559dfbf8dfc0465a2078b` |

## 4. 确定性与模型门槛

| 检查 | 实测 | 判定 |
|---|---|---|
| 最终单元/接口测试 | 991项，退出0，64.328秒 | 通过；990项旧运行保留，补充未合并查询的预算测试后重跑991项 |
| 公开 dev / validation / blind_v2 / holdout | 80/80、80/80、80/80、78/80 | 四套逐题零新增回归；公开V2历史基线57/80 |
| 本次独立路由 | 77/80；macro96.25%；最低类8/10；边界38/40 | 整体、各类和边界门槛通过 |
| 公开V2回答三轮 | 每轮39/40，ASR95%、FRR5%、不可答拒答100%、边界100%、引用编号100%、事实95%，稳定性100% | 每轮八项门槛通过；006三轮仍失败 |
| 独立回答三轮 | 自动每轮40/40；严格语义每轮39/40 | 020附加审批关系不完整另计；原始评分器未改 |
| 历史交付复现 | 8个脚本全部退出0 | 通过 |
| 历史撤销探针 | 3/4/5/6/7/8/9/final分别5/5、4/4、5/5、2/2、4/4、5/5、6/6、3/3；11最终18/18 | 均有断言失败，非导入/执行错误 |
| 本次新增撤销探针 | 7/7检出，errors=0 | 逐项列出目标测试、替换内容与实际失败数 |
| 前端生产构建 | Node24.15、Vite8.2.2，18模块，成功 | 前端代码未改；不是浏览器端到端测试 |

probe11最初17/18的输出保留：新第二处预算防线掩盖了撤掉第一处防线的旧合并查询测试。原断言保持，通过新增未合并的三分句用例单独保护第一处预算入口，probe最终18/18。新增7项撤销的G运行两个测试，实际只有一个失败；只声明该撤销被检出，不声称每个测试都变红。

公开与独立回答共240条原始记录，保留每条实际来源。无损去重为48条和59条，由Claude完整复核；主验收方验证映射及来源顺序，并逐条裁定有争议指控。独立020省去“还需”按保守完整性口径记录，不推断为明确免除第一审批人。详情见 `results/semantic/REVIEW.md`。

## 5. 三路径与复合覆盖

冻结20题：静态Wiki Top-1为18/20，Document为19/20；动态六次独立编译后均20/20。三路径Hit@3均20/20。合计固定六次为 `[57,57,57,57,57,57]/60`，中位数、最小值和最大值均57/60（95%），6/6达到预设54/60，所有单路径底线通过。

六次编译实际内容哈希全部相同：`afba8ff6258dc2ec0541d57f14668cae997f875b76125f04e007ba57c57035e1`。它们是六次新编译执行，只有一种内容，不能证明历史编译方差已经消失。新题集替换了曝光输入，也不能把历史55/60→本次57/60归功于算法改进。

静态Top-1漏项为 `accept_retrieval_008`、`accept_retrieval_closeout_020`；Document为 `accept_retrieval_closeout_v2_005`，前三条仍命中。原始结果见 `results/final-summary.json` 的8b身份数据和各编译目录。

公开复合12题×3轮，72条检索测量均遵守证据上限和三个子查询上限：

| 预算 | 接管前R11（6f） | 中间8b | 最终48f | 本组预算内上界 |
|---|---:|---:|---:|---:|
| k=3 | 92.50% | 88.33% | 93.47% | 94.58% |
| k=4 | 94.17% | 98.33% | 98.33% | 98.33% |

中间版满额时无法补齐造成的 `r2_cov_11` 回归已修复。剩余 `r2_cov_08` 第1轮k=3只选18，尚有两个空槽，19已召回但模型未选择、词面分数2.7701未过3.0阈值。这是可避免的选择漏项，不是必然预算损失。原始模型输出与同选择输出在两版的重放均保留于 `results/remaining-gap-replay.json`；“旧版也漏”仅排除新增回归。

**对此缺口的正式处置**：保持3.0阈值和弱匹配不填充行为，避免为填槽把“还能顺延”“多久失效”等模糊问题补成错误主题；接受该1/36次k=3的有限剩余风险交付，保留开放质量问题，不声称已修复全部覆盖缺口。默认k=4在本组达到预算内上界。此裁定不修改原先的数量/调用边界或固定集门槛，也不追加抽样直到通过。

## 6. 最终真实API、性能和更新

2026-09-15晚按事先记录的替代协议运行：基线和最终候选各12题×普通/SSE，固定一轮、同暖机、同模型；无并行编译。外部requests观察器对两侧把Ollama地址映射为IPv4 loopback，源码、提示词、模型返回值和评分器不变。绝对时间仅适用于这一连接环境。

| 最终候选 | 普通 | SSE |
|---|---:|---:|
| 自动可答 | 8/8 | 8/8 |
| 严格语义可答 | 7/8 | 7/8 |
| 固定边界/缺资料拒答 | 4/4 | 4/4 |
| 路由不符 | 0 | 0 |
| 最多生成/复查次数 | 2 | 2 |
| 三道固定能力边界生成次数 | 全0 | 全0 |

主验收方阅读24条实际请求无损合并后的14条正文/来源组合。020两侧的额外审批关系仍不完整，SSE还略去系统提交方式；本批实习生不享带薪年假两侧均保留，不能照搬旧8b批次的遗漏。其余七题的关键事实、完整SKU、单位、条件与实际引用通过。详见 `results/semantic/final-api-review.md`。

所有24条请求核对终态done、实际SQLite正文；12条SSE全部未经strip的delta拼接严格等于最终正文。相同脚本化模型响应下两接口严格一致由最终接口测试独立保证，真实模型允许措辞差异。

| 配对性能 | 基线 | 最终候选 | 增幅 |
|---|---:|---:|---:|
| 普通端到端p95 | 3.1259秒 | 3.2537秒 | +4.09%，通过25%门槛 |
| SSE端到端p95 | 2.8753秒 | 3.2518秒 | +13.09%，通过25%门槛 |
| SSE首段正文p50 | 0.3104秒 | 1.8336秒 | 单列 |
| SSE首段正文p95 | 1.3888秒 | 3.2990秒 | +137.54%，单列 |

端到端p95的样本为每侧全部12请求，首段正文为其中8道可答请求，分母不同；均沿用原百分位计算。无条件缓冲会延后正文，不能用它豁免端到端25%门槛，也不能隐去本次首段成本。样本小且仅一组预定配对，不给生产延迟保证。

新基线普通缺资料拒答为3/4：025交付模型过程自述。该失败保留，不因不满足候选质量线而重跑。较早完整8b配对的普通/SSE总耗时p95增幅+1.75%/+2.65%、首段p95+16.91%只作历史记录，不能替代最终48f的新测量。

最终上传更新演练两次真实发布：build-0001→build-0002，Wiki与原文路径均将正式员工满一年年假从5天切换为6天。检查实际路由、完整年假事实、引用、旧事实不再进入来源，以及数据库正文；四次查询均通过。两次发布耗时约26.34/32.43秒，仅为本次观察。没有更新真实资料库。

## 7. 保留的语义问题与范围

最终复合36条回答保存各自实际来源，无损合并为22条，全量审查完成；不能用另一条检索请求的来源替本次回答证明引用。逐例证据在 `results/semantic/compound-review.md`：

- 01把年假失效/顺延答成调休期限与“不计入加班”；02第3轮无依据声称“不支持预支”。
- 04/05/12压缩了额外审批关系；09漏掉迟到按“每月”累计；10漏掉“未经公司批准的”，将限定禁令扩大到所有公共网盘。
- 08多引设备领用段；11第1轮绩效四档只引申诉段，第3轮多引考勤段。
- 03的五个独立资料段超过默认四个槽，三轮只覆盖四项；不得要求不可能的满覆盖，但不能把“本次未检索到”说成整个制度没有规定。
- 公开 `answer_v2_006` 稳定拒答仍计失败和误拒；独立/API的020附加审批关系不完整仍计失败。

复合回答自动事实覆盖94.17%是关键词计分，既未捕捉所有语义问题，也不能称完整回答正确率。11题只回答匿名本来符合来源，冻结正则却只认实名；09原文对单次/累计分钟的表述有歧义，不另造确定错误。自动成绩与人工裁定分开保留，评分器没有改。

这些问题阻止宣称复合语义可靠或零幻觉。协议未设置这组已曝光12题的零缺陷门槛；本阶段以工程约束和原先固定集门槛通过、上述剩余风险明确披露的范围完成，不把持续增加语言词表作为无穷返修。本报告不替代后续更强语义能力的验收。

未覆盖：冷启动、多实例并发、前端浏览器端到端、真实企业数据源、其他Embedding模型。问候和否定作用域对词表外表达仍有泛化限制；历史跨会话波动和编译方差不能因本次未出现而销项。当前System仍为内存样例库存，只读，不含企业接入或写操作。

## 8. 失败批次、探针与文档勘误

最初未完成批次的状态/局部日志保存到 `results/interrupted-*`。工具session消失不等于已证明操作系统进程退出，未落盘的题级结果不能还原，因此该不完整批次不采信。之后复合固定三轮全部完成并保留，没有挑选最好一轮。

随后最终API基线完成10条请求后发生180秒Ollama读取超时，服务日志也记录3分钟HTTP500；120.692/132.640秒等已完成延迟和完整错误日志均保留在 `results/final-api-baseline`。这不是选择性丢弃低分，也不拿异常慢基线制造加速。晚间服务已退出，恢复相同版本/digest后按 `REPLACEMENT_API_PROTOCOL.md` 运行且仅运行一组新配对。localhost连接开销不足以独立解释服务内部停顿，完整原因尚未确定。

Wiki排序键的第二项只裁主分数相等的记录，跨页但主分数相等时也可能重排；历史“只裁同页/跨页绝不变”说法过强。保持运行时字节身份，在当前README与本报告更正。`document_adapter.py` 的旧docstring关于纯相关性截断、仅空位补齐已被本报告的覆盖优先/满额修复说明取代，不能将旧注释当成实际机制。本阶段未修改这些adapter运行时文件。

`rag.py`保持2093个CRLF、0个裸LF；三份当前文档保留原CRLF。Git对另外三个已有LF文件给出未来转换提醒，未因此重写它们。`git diff --check`退出0。

## 9. 复验入口与交付

所有Python检查使用原项目 `.venv/Scripts/python.exe`，模型/API测试均在隔离源码副本。只切换cwd不能隔离API默认数据库。

| 检查入口 | 结果与记录 |
|---|---|
| `python -B -m unittest discover -s tests -q`（candidate-final） | 991项，退出0；`results/final-deterministic/unittest-strengthened.txt` |
| `evaluate_orchestrated_routes.py --dataset ... --output ...` | 五套退出0；`results/final-deterministic/route-*.json` |
| `docs/m10-selftest/repro_rework*.py`、`revert_probe_*.py` | 全部最终记录见 `results/final-deterministic/`；probe11采用 `revert-11-final.txt` |
| 本次7项撤销 | `results/final-closeout-probes/runs/20260915T044840Z-1578d9/REPORT.md`及summary.json |
| `tools/prove_unaffected_paths.py` | 224次检查；`results/unaffected-paths.json` |
| `tools/run_observed_coverage.py` | 固定三轮退出0；`results/final-coverage.json`、回答与HTTP原始jsonl |
| `tools/observe_loopback.py api ...` | 基线/候选各退出0；`results/final-api-*-ipv4/` |
| `tools/observe_loopback.py update` | 退出0；`results/final-update/` |
| `tools/audit_final_api.py`、`inspect_update_closeout.py` | 最终原始事件、数据库与来源审计均通过 |
| `tools/final_integrity.py` | 实时源码、被测副本、冻结题集、模型、真实数据库、HEAD和暂存区检查通过 |
| 前端构建 | `results/frontend-dist/`为实际构建产物；源码与依赖未改 |

广泛模型答案、六次编译及全部原始数据位于 `results/`，每份记录保留原候选身份；`results/final-summary.json`是8b批次汇总，`results/final-revision-summary.json`是48f最终批次汇总，不能混用。历史报告全部保持原样。

交付目录为证据根目录下 `delivery/`：`m10-source.zip` 包含Git跟踪文件及全部非忽略的未跟踪源码/测试/文档；`manifest.json`记录每个文件的磁盘原字节和LF归一哈希、源码包及patch哈希；`working-tree.patch`、`untracked-files.json`、`git-status.txt`保留恢复材料。源码包逐成员CRC与SHA校验，包含未跟踪文件；仅导出HEAD无法恢复本候选。

该源码快照不含真实数据库、模型或依赖二进制、Git历史。它可用于恢复误改，不能代替独立介质上的硬件故障备份。没有提交、推送、合并；是否创建Git提交仍由用户另行决定。

相对HEAD：9个已修改文件、213个未跟踪文件，暂存区为空。相对9月12日接管快照的本阶段变更已在第1节单独列出；两种口径不能混算。

```text
 FRONTEND.md                       |   10 +-
 README.md                         |  256 ++++--
 chat_orchestration.py             |   16 +-
 docs/DEMO_GUIDE.md                |  117 ++-
 orchestration/document_adapter.py |   38 +-
 orchestration/planner.py          |  425 +++++++++-
 orchestration/wiki_adapter.py     |  186 ++++-
 rag.py                            | 1631 +++++++++++++++++++++++++++++++++++--
 tests/test_streaming.py           |   14 +-
 9 files changed, 2504 insertions(+), 189 deletions(-)
```

已修改文件状态如下；完整未跟踪文件清单与原始git状态随交付保存。

```text
 M FRONTEND.md
 M README.md
 M chat_orchestration.py
 M docs/DEMO_GUIDE.md
 M orchestration/document_adapter.py
 M orchestration/planner.py
 M orchestration/wiki_adapter.py
 M rag.py
 M tests/test_streaming.py
```
